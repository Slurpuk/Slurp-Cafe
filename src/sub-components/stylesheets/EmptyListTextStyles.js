import {StyleSheet} from 'react-native';

export const EmptyListTextStyles = StyleSheet.create({
  emptyText: {
    paddingTop: '15%',
    paddingHorizontal: '2%',
    textAlign: 'center',
    fontFamily: 'MontSerrat-SemiBold',
    color: 'grey',
    fontSize: 30,
  },
});
